# STATE — the operator's state file (regenerated at every verdict; a fresh session starts from THIS, not from the handoff history)
_updated 2026-09-19 00:05 UTC_

## Fleet
- 80 bots / 16 Peaceful worlds. **b1659c0 fleet-wide, ONE version live on all 80, no canary.** Verified 16:41:35Z after owner-01b's teardown. main = b1659c0. Previous mains: main-pre-2026-09-17 (1d6c97d), main-pre-2026-09-16b (08a3da2), main-pre-2026-09-16 (426058d).
- Health at 11:00Z digest: one version live pre-canary, 80 bots, 0 deaths in 2 h, 0 immobile, items/bot-h 31.5, decisions/bot-h 45.3.
- **keepInventory=true and doImmediateRespawn=true on every world** (deliberate, place-town.py): deaths cost time, not items. Owner decision 18 Sep 02:10Z: it stays ON through the seed canary's 72 h, then is turned OFF fleet-wide as its own registered program change (a world rule, never a canary).

## No live canary — the slot is FREE and the ledger is closed
Both of today's movement-owner attempts are closed. **The change was never shown to be unsafe; it failed twice for two different instrument reasons.**

### owner-01 (aa44514) — INCONCLUSIVE 12:59Z, SHIPPED INERT
97 minutes of baseline under a new sha: `OWNER=1` never reached the process, because `mcai-canary-tree` could write only `CODE_VERSION` and `RUN_ID`. Every other check was green. Fixed (`CANARY_ENV` + a `/proc/<pid>/environ` assertion in `fleet-deploy`); see "Rules changed today".

### owner-01b (aa44514) — REVERT 16:38:45Z, torn down 16:42:21Z, verified one version live 16:41:35Z
Reverted on `('hive-a-Bravo', '16:33:01', 'escape_rung')` — "change row inside a death window, discriminating". **The mechanism does not support it and v10 already forbade it.** Every rung inside the fatal episode was `outcome=refused` or `outcome=preempted blocks=0`: the owner placed nothing and moved nothing while the air reflex held the body at priority 100. The death is the drowning pipeline documented today in `drowning-pipeline-2026-09-18.md`, textbook — four ceilings, ~93 s, and a yield declaring **"no harm" at health 3.33**, inside the 3.17-4.67 band whose 23 members were all fatal. **Control board-c-Bravo died identically four minutes earlier** (16:28:39, *drowned; idle; hp 1.2->0 over 3s*), and all three fleet deaths in that 2-h window were drownings-while-idle across both arms.
- Before the revert it was healthy: exposure **68 episodes**, all v15c guards within at +180 (blocks moved +0%, working share -4%, items -13%, immobile DiD -0.4 pp), two WATCH lines with no harm behind them, and the **v21 death gate correctly holding a 10.00x point ratio at a 0.78x lower bound** on its first live test.
- **v22 drafted (prospective, commit 37839eb, in the registration doc).** Two defects: v19's "refused if the row appears in a CONTROL death window" is **vacuous for a change row**, which never appears on control at all — it reads as discrimination and performs none; and v10's *"counts only a rung that MOVED the body ... refusals and terminal states are reported, never linked"* was not applied in the v19 licence path. v22 restates v10 inside that path and requires the row to be in the **same episode** as the death.
- The REVERT stands (gates are honoured, amendments prospective). On the evidence the honest verdict was **INCONCLUSIVE**.
- **The standing-directive question is now moot for today but returns with owner-01c:** "ARBITER stays OFF" vs `config.mjs:155` making `OWNER=1` imply it. I read it as the fleet default rather than a bar on the canary built to test it. **Get the owner's word before the next attempt.**


## Closed today
- **owner-01 (aa44514) — INCONCLUSIVE 12:59Z, torn down 13:02Z, SHIPPED INERT.** It ran 97 minutes on hive-b,hive-c as the baseline under a new sha. Ground truth on the live process (hive-b-Alpha pid 756887, 57 env vars as the positive control): `RUN_ID=owner-01` present, **`OWNER` and `ARBITER` absent**. `canary.env` held only `CODE_VERSION` and `RUN_ID` because `mcai-canary-tree:46` hardcoded `printf 'CODE_VERSION=%s\nRUN_ID=%s\n'` and **had no mechanism for any third key**; `config.mjs:157` defaults `owner` to 0. Every other signal stayed green throughout: sha split confirmed, suite 192/192, preflight `changerowcheck` PASSED, safety lines clean (0 canary deaths vs control 0.027/bh).
- **The tell was the exposure counter, and it was readable at +90.** `episodes_canary = 0` against `min 1`. Entombment episodes run **1.5/bot-h fleet-wide** (24-h read: 2,885 episodes over 80 bots; 157 on hive-b, 183 on hive-c), so 10 bots over 90 minutes expect **~22** and P(zero) is **~0%**. A reported zero at that rate is an instrument or a dead code path, never a quiet world. Left alone, `final_on_zero_exposure: INCONCLUSIVE` would have closed the day at 00:25Z with nothing learned and no one knowing why.
- Teardown verified: drop-ins removed (0 remain), manifest cleared, all 10 pool bots restarted 12 s apart 12:59:58-13:01:49Z, **one version live on all 80 bots** (`b1659c0+ab74e7`) before the redeploy.
- **falls-01 (fc28885) — REVERT 04:39:10Z, torn down 04:42:45Z, by the loop, before this session started.** Ledger clean, no open loop, one version live. The verdict stands; the read of why is in the registration doc under v21, and it is the reason the death gate changed.
- **THE DEATH GATE REVERTED A CHANGE THAT COULD NOT HAVE CAUSED IT.** falls-01 was report-only — a `fall_path` row at a fall, no behaviour change. Its two deaths were idle (one drowning, one unknown, **zero falls**); all **three control deaths in the same window were idle drownings**, the same background mechanism. Denominators: canary 2 in 23.9 bot-h (0.084/bh), control 3 in 168 bot-h (0.018/bh); point ratio 4.7x, lower 95% bound **0.58x**.
- **falls harvest (31 `fall_path` rows, placebo-a 19 / placebo-b 12, zero fall deaths):** the liquid-dropdown hypothesis is **dead** (liqdrop=true on all 31, landing cell water on **1**). 29 of 31 are the planner's own 4-5-block drops at 1-2 hp, which is policy (maxDropDown 6), not defect. **One overshoot** (placebo-b-Comet 03:49:24, planned 5.3, hp -9 => ~12 blocks — pathfinder momentum, issue #31). **One real infinite-liquid dropdown** (placebo-b-Echo 03:14:22, planned max **49**, 1 drop beyond mdd=6, into water, cost 2 hp — the mechanism exists, is rare, and is harmless when the water is there). **The fatal 35-block class is still unmeasured**: it needs a fall DEATH, and 2.4 h on 10 bots produced none.

## Rules changed today (both PROSPECTIVE, both in docs/reports/recovery-ladder-registration.md)
- **The tier-1 analyst has been reading two DEAD runs since 16 Sep.** `analyst.py` globbed `~/digest/reads/*.out` while `canary-loop.sh:53` writes `$RUN-$s-$M.txt` (and `.json`) — the extensions never matched. Measured today: the directory holds **6 `.out` files, all dated 16 Sep for recovery0809 and recovery1011**, against **75 `.txt`** including the live run's. So every analyst verdict since the loop switched extensions carried two-day-old reads from finished trials under the heading "latest canary reads", beside a digest header naming the current run. It produced **two confident false alarms on 18 Sep alone**, including "the +30 read for owner-01b is missing" about a read taken 20 minutes earlier. Fixed: the glob is scoped to the manifest's `run_id` and takes `.txt`, and when the live run has no reads yet the analyst is **told exactly that** rather than handed a neighbour's — substituting another run's reads is worse than showing none. Five selections exercised including both empty branches; no cross-run leakage. Backup `~/analyst.py.bak-20260918`. It remains SHADOW (it never decides), so this was noise rather than risk — but it is the third instance today of an instrument answering about a world that no longer exists.
- **The OpenLoop guard could be pre-satisfied by the PREVIOUS trial's verdict, and was, for 26 minutes today.** `open_loop()` matched a decision to a deployment by **sha alone**. owner-01 (aa44514) was recorded INCONCLUSIVE at 12:59:51Z; the same sha redeployed as owner-01b at 13:04:27Z; and `check-open-loop.py` then answered **"no open canary — clear to start something new"** while that canary was live on board-b,hive-a. The one tripwire under "Done means read on the fleet" was off, and it would have green-lit a SECOND canary — three versions and a halted fleet. Redeploying a sha is routine (a flag fix, an env fix, a bad draw), so this was not exotic.
  **Fixed: ordering, not identity.** A decision closes a deployment only when its `ts` is at or after the manifest's `declared_at`. Fails closed throughout, as the module's stated inversion requires — a decision with no `ts` cannot prove it belongs, and a `declared_at` that is present but unparseable is an OPEN loop (somebody edited it; that is when a human should look). An ABSENT `declared_at` falls back to the old sha match, so a manifest older than this check cannot deadlock the pipeline. **12 new behavioural tests, 5 mutants killed** (including the live bug and an off-by-one at the boundary), existing suite still 33 assertions green. Verified live: the ledger now reports owner-01b OPEN. Installed to `~/mcai-analysis/lib/openloop.py` (the golden copy, restored after every deploy) as well as `/opt`; backups `~/mcai-analysis/lib/openloop.py.bak-20260918` and `/root/openloop.py.bak-20260918`.
- **A canary behind a feature flag can now carry the flag, and cannot ship inert silently.** Two mechanism fixes, both on .31, both backed up (`/root/mcai-canary-tree.bak-20260918`, `~/bin/fleet-deploy.bak-20260918-flags`):
  1. `mcai-canary-tree build` reads **`CANARY_ENV`** — whitespace-separated `KEY=VALUE` pairs appended to `canary.env`. It refuses `CODE_VERSION=` and `RUN_ID=` (this script's to own; a canary that relabels itself is worse than one that does nothing) and refuses anything not matching `[A-Z_]*=*`. **Seven cases exercised and all four refusals seen to fire**, not just asserted.
  2. `fleet-deploy` passes `CANARY_ENV` **through the `sudo`** (sudo builds a fresh environment, so an exported flag vanishes before `deploy-fleet.sh` reaches canary-tree) and, after `VERIFIED` on a `--pool` deploy, **reads `/proc/<pid>/environ` of a live canary bot and exits 4 with `FLAG MISSING: ... THIS CANARY IS INERT` if a declared key is not there.** It checks the env dump is non-trivial first, and reports INCONCLUSIVE rather than passing when it cannot read it — an unreadable dump is not evidence of presence.
- **v21 — the death gate's 1.25x test moves to the LOWER BOUND of the rate ratio.** The owner's two-death floor is untouched. The gate is polled every 5 min for up to 9 h (~108 looks) at an event with a null expectation under one death per canary, so the point ratio clears 1.25x on Poisson noise. Calibrated before it may decide anything (`scripts/calibrate_deathgate.py`): **false revert 43.7% -> 5.0%**, detection at 3x 95% -> 51%, at 5x 99.9% -> 88.6%, at swim_to scale (10x) 100% -> 99.9%. The 43.7% is the positive control — it reproduces the 46% measured on 2026-09-13. Rejected alternative: excusing a death whose cause class also appears in control, which would have excused swim_to. `scripts/deathgate.py` (pure function, 15 tests, 5 mutants killed), wired into `~/verdict.py` on .31 (backup `~/verdict.py.bak-20260918-deathgate`).
- **The analysis library no longer belongs to the deploy tree.** Every read does `sys.path.insert(0, '/opt/minecraft-ai/scripts')`, but `/opt/minecraft-ai` is a deploy artefact that `fleet-deploy` checks out and `deploy-fleet.sh` git-resets — so the library silently became whatever the last canary carried. Measured today: deploying owner-01 reverted a telemetry fix installed 40 minutes earlier and would have broken owner-01's own +30 read. **Golden copy now lives at `~/mcai-analysis/lib/`, and `~/bin/fleet-deploy` restores it after every deploy** (backup `~/bin/fleet-deploy.bak-20260918`). Verified in place after the owner-01 deploy.

## Instrument fixed today — `Events.load()` was refusing EVERY call
`scripts/lib/telemetry.py`, commit c79d57b. The OOM guard summed the whole glob before applying the window, so 1124 rotated generations (0.44 GB on disk, counted at 25x) estimated 11.4 GB against 1.9 GB actually there, and `since_minutes=30` raised exactly like a 30-day walk. It broke between 04:39Z and 11:10Z today and took fallread, deathread, canary-report and verdict.py's evidence objects with it — i.e. it would have broken the next canary's reads entirely. Fix: one `predates_window()` shared by the estimate and the walk, one frozen glob for both, a runtime decompressed-byte budget, `WalkTooWide` re-raised instead of swallowed, **one bad line now costs a line rather than the rest of the file** (Codex reproduced valid / `[]` / valid returning ONE row), unreadable files and mid-walk rotation reported rather than silent, naive datetime bounds refused. 16 behavioural cases, six mutants killed. Two Codex passes folded; two of the tests were caught passing for the wrong reason by their own mutants first.

## SEED CANARY — POOL ONE IS LIVE (19 Sep 00:00:44Z)
- **placebo-a reseeded**, `level-seed=8948499624371160708`, town at **249,-144** (y=74, relief 3, 0% wet within 32; 47 candidate sites rejected), pregen 1024x1024 in 48 s, envs rewritten `HOME 249 75 -144 / BOARD 252 74 -144`, **5/5 confirmed in-world by RCON**. Old world and town record archived as `*.pre-reseed-20260918T235209Z`; bot state archived alongside. Draw-exclusion stamped to **2026-09-22T00:00:44Z**.
- **Draw (auditable, reproducible from the record):** eligible after the 12-h ledger exclusions (board-b, hive-a, hive-b, hive-c) and the standing bans (isolated-*, placebo-c) were board-a/board-c/placebo-a on the 5080 half and board-d/hive-d/placebo-b/placebo-d on the 3090 half. Lowest SHA-256 of `<pool>+2026-09-18-seed-canary` in each half: **placebo-a** and **placebo-b**.
- **The 72-h window closes 22 Sep 00:00Z**, clear of the 24-27 Sep program read — which is why Claude's review made this timing load-bearing.
- **POOL TWO (placebo-b) IS NOT STARTED.** The owner's protocol is pool one, then verify 5/5 in-world **and a clean digest an hour later**, then pool two. 5/5 is confirmed; **the digest check is due after 01:00Z**.
- **`reseed-pool.sh` took SIX fixes tonight, all committed with evidence.** Four found by running it (a clean SIGTERM leaves the unit `failed` not `inactive`; `list-units` omits stopped units so a resume counts zero bots; `grep -c .` aborts under `set -euo pipefail` before its own diagnostic; an `is-active` precondition that refused the operation it was halfway through). Two found by a Codex audit of the resume path and fixed before pool two (`server-up` dating its log window from this attempt so a resume always times out; `town-placed` replaying a placement that `place-town.py` refuses to stamp twice). **Every one is the same shape: a check written for a clean start, met on a resume.**

## Queue — REORDERED 23:55Z after the two-engine review (`review-reconciliation-2026-09-18.md`)
Both engines reviewed independently and converged. **Both rejected my headline proposal.** The order below is theirs, not mine.

1. **Seed canary — FIRST, and the timing is now load-bearing.** `scripts/reseed-pool.sh` v3, unsupervised, **one pool at a time**: pool one, verify 5/5 in-world and a clean digest an hour later, then pool two; stop on any stage that fails to verify; nothing is deleted (archives `.pre-reseed-<ts>`). Registered in `seed-canary-registration.md`. **Start it early on 19 Sep so its 72 h closes well before 24 Sep** — Claude caught that re-seeded pools sitting inside the 24-27 Sep program read window are a world-level confound on gather and stock. If it cannot close before 24 Sep, exclude the re-seeded pools from the program read explicitly and say so in the registration.
2. **Implement v23 in the verdict path, with a replay acceptance suite.** v23 is registered (no single canary death reverts by any path); the code has not changed yet. ChatGPT's bounded version: **one engineer-day, then stop adding harness scope.** Build the suite from incidents already on record, each of which must come out right: inert deployment → invalid experiment, close early; wrong-run or stale evidence → unreadable; undefined primary endpoint → unreadable, **not** an efficacy failure; background drowning + irrelevant rung → report, never an automatic linkage revert; a changed refusal blocking an executable rescue → retain a harm signal; an injected known regression → REVERT. Make uncalibrated linkage WATCH-only until the suite passes. **Costs zero fleet time.**
   **Reconcile the executable artifact first:** ChatGPT found `scripts/verdict.py` in the repo still carries the point-ratio death test while `~/verdict.py` on .31 imports `deathgate.death_gate`. The host is the live one (verified: the +180 read printed the lower bound), but two copies disagreeing about the rule is how today happened twice.
3. **NAVIGATION / GATHER — promoted into the queue, where it has never been.** This is the only committed metric that is failing: **20.3% against a 40% two-week gate**. Claude's arithmetic decided the ranking — navigation-class failures run ~7.6/bot-h, so a 10-bot 6-h canary sees **~455 events** against drowning's ~12. **~38x the exposure, on the metric that actually fails.** Upstream has already characterised the territory, which makes it cheaper than assumed: `mineflayer-pathfinder` documents that it does not dig, place, swim or parkour, and issues #222 (hangs when obstructed by a block it cannot break), #273 (stuck with an active goal on an incomplete path) and PR #380 (walks stopping under an unreachable goal) are the 14,544. **Do not require the movement owner as a prerequisite unless that dependency can be demonstrated — "program step 3" is a plan, not evidence.**
   **Denominator corrected:** `unreachable` 9,940 + `no_path` 4,604 = **14,544 of 23,544 (61.8%)** are reachability failures. Adding `no_safe_target` (7,726) gives 22,270 (94.6%), but that class is a *safety* refusal ("candidates are beside water or under falling blocks"), not a navigation failure. The memo listed three classes and summed two; both engines caught it.
4. **owner-01c — the movement owner, third attempt.** It has never failed a guard; it has failed the apparatus twice. Needs: v23 live (item 2), the owner's word on ARBITER, and a draw with **headroom on the primary endpoint** (item 8).
5. **DROWNING — demoted from 3, and the design has changed.** Its read-corruption argument **evaporates once v23 lands**, because a background drowning can then no longer revert anything; what remains is pre-investment for `keepInventory` off, plus an unmeasured interrupt tax. **Do the one-hour read of that interrupt tax before spending a canary slot** (memory says the air reflex once aborted 9.8% of every run; nobody has recomputed it). Work cost of the deaths themselves is 28 x 104 s = 0.81 of 1,920 bot-h = **0.04%**.
   **The health floor moves: the separation is "not at full health", NOT "below 5".** My drowning report accounted for 258 of 291 yields and dropped 33. Re-read 23:40Z (24 h; windows rolled, so totals differ from the morning's 291/27): **under 5 → 19 yields, 100% fatal; 5-10 → 1, 0%; 10-15 → 3, 67%; 15-19.9 → 27, 70.4%; full → 271, 0.7%.** The omitted band is larger than the reported one and 70% fatal. A floor at 5 misses it.
   **"The proven pieces" are half-proven.** `floatDigTargets()` returns `[]` unless `path[0].toBreak` is non-empty (`digapproach.mjs:481-482`) — it needs a **pathfinder plan**, and the drowning rescue has no path, so the remedy must synthesise its own target (the lid above the head): new code, no fleet history. `floatDigOk()` is reusable but allows water adjacency by design, which is wrong under an ocean. The `_goto_float_dig` control (72 fires, 0 deaths) is **selected** — those bots had a path, i.e. were not sealed, which is the population being treated.
   **Endpoint rewritten.** Ceilings-per-episode is rejected by both engines: it can **reward failure** (fewer ceilings may mean an earlier death) and has ~3 canary episodes, i.e. no power. Use *reached and sustained breathable space within a fixed horizon per eligible episode*, death or continued entrapment as failure, incomplete observation censored; report the **rate**; pre-register the MDE (≥50% marginally detectable, 25% not). Consider shipping the floor **fleet-wide as a program change** rather than burning a slot on an endpoint that cannot move. **Do not bundle the floor with the dig** — they interact, so a revert would teach nothing about either.
6. **falls-02 — the falls instrument FLEET-WIDE.** Behaviour-inert, no canary slot, runs alongside anything. Denominator from today: **7 fall deaths at 23/31/31/36/40/40/42 blocks** of 48, ~0.0036/bot-h.
7. **`keepInventory` OFF — MOVED TO AFTER 27 Sep.** Claude caught that seed-canary-plus-72h lands it ~21-22 Sep, **two days inside the two-week read window**, changing what a death costs mid-read. It is a program change, it confounds the deaths line, and it waits.
8. **Extend `drawexposure.py` — two checks, NOT a new script.** (a) a **non-degenerate pre-period** on the primary metric: owner-01b drew board-b and hive-a at a **0.0% immobile pre-share**, so its primary ratio-DiD was a divide-by-zero printing `+nan% FAIL` — you cannot reduce immobility from zero; (b) **exposure against the MDE-implied n**. Building these as a separate `gate-power.py` was rejected by both engines: it would repeat the drift defect (two instruments that must agree about the draw). Still REPORT-only until calibrated against draws whose outcome is known (-13b, -13c, 1011b).
9. **Fix `programread.py`'s stock-returned units BEFORE 24 Sep.** The program commits to **items/bot-h returned to stock**; the script reports **deposit success rate**. Different quantities. If it is not fixed the line is unreadable in the window that judges it — the same class as owner-01b's `+nan%` endpoint.
10. **Add one line to the nightly job: verdicts per canary-day.** Claude: "two canaries, zero results" is the throughput complaint and nothing measures it — the same failure as the two program metrics that had no standing read.
11. **Pocket-rung block floor** (need+4 with 5 held). **12.** Pooling rule -12 (iron). **13.** Housekeeping: delete `~/mcai-analysis/registrations/exptest.json` on .31.

## OWNER DECISIONS — BOTH TAKEN 2026-09-18 23:35 UTC
- **ARBITER STAYS ON for owner-01c.** Owner's word, 18 Sep. `OWNER=1` (which implies `ARBITER=1` via `config.mjs:155`) is authorised for the movement-owner canary. The ambiguity with the standing "ARBITER stays OFF" line is resolved: that line is the **fleet default**, not a bar on the canary built to test it. No need to re-ask.
- **CONCURRENT CANARIES: APPROVED, build it.** Owner's word, 18 Sep. The tripper is to accept a **set** of declared canaries instead of exactly one, so 2-3 **disjoint** canaries can run at once. Design and blast radius below; implementation is the next build.

## (superseded) OWNER DECISIONS WANTED
- **ARBITER.** The constraints say "ARBITER stays OFF"; `config.mjs:155` makes `OWNER=1` imply it. I read that as the fleet default rather than a bar on the canary built to test it, and proceeded today. owner-01c needs your word.
- **Concurrent canaries — the only idea on the table that changes throughput by a multiple.** CLAUDE.md's "one canary pool, ever" is justified by *"the tripper matches `canary_pool` literally"* — an **implementation limit, not a statistical one**. With 80 bots over 16 worlds, 2-3 **disjoint** canaries could run at once if the tripper accepted a **set** of declared versions instead of exactly two. ~1-2 days, and it touches the fleet's halt protection, which is why Claude flagged it for you rather than for me.


## Rules in force (docs/reports/recovery-ladder-registration.md)
**v23 (no single canary death reverts by ANY path; gates carry `role: deciding|tripwire`)**, v22 WITHDRAWN unregistered (both reviews rejected it; it was vacuous for 58% of deaths), v12 linkage, v14c, v15c movement guards (calibrated 2% false-revert / 97% detection), v16 (calibrate before revert; trace refusals to fallbacks; histogram slot order; bundles of two disjoint changes), v17/v18, v19 (a change/linkage row must DISCRIMINATE — `changerowcheck.py` pre-deploy, `license_change_rows()` at read time, the single-death rung-linkage override demoted to WATCH), **v21 (the death gate on the lower bound)**, the owner's death gate floor of **two** canary deaths, draws at deploy (12-h ledger exclusions, ±25% then ±40%, 5080 half, never placebo-c/isolated), `fleet-deploy` refuses a `--pool` sha not descending from `declared_code_version`.

## Known false alarm — the analyst pages OpenLoop for the whole life of every canary
`check-open-loop.py` reports OpenLoop whenever the manifest declares a canary with no decision
recorded, which is the NORMAL state of a live canary from deploy until verdict. The tier-1
analyst pages on it every 30 min: four times during -13c (17 Sep 18:30/19:00/19:30/20:30), twice
during falls-01 (18 Sep 03:30/04:00), and it will do so ~26 times across owner-01's 13 hours.
**It is not an alarm while the loop is alive and the deadline has not passed.** The
discriminating test is `pgrep -f canary-loop` plus the deadline: OpenLoop with **no loop
running**, or past `deadline_min`, is the real thing. Queued (not done today, because editing
the analyst's rule while it watches a live canary is the wrong moment): give the analyst that
two-part test so OpenLoop pages only when it means something.

## Standing wake-ups
- **Nightly 00:12Z: the five program numbers** (`~/programread.py 24` -> `~/digest/programread.log`, installed 18 Sep; crontab backup `~/crontab.bak-20260918`). Built because only THREE of the five had a standing read: the 30-min digest carries deaths, immobility and the retired items/bot-h; ironfunnel carries the iron share; **gather success and stock returned had no standing read anywhere**, which is how gather moved 30% -> 20.3% unobserved. Prints both deposit denominators on purpose, splits by `code.version` so a promotion cannot blur a window, and REFUSES rather than printing zeros if the walk looks broken. The per-version split is NOT a canary read — no DiD, no exposure cut — and says so in its own output.
- **Declare the two-week program read window: 72 h continuous, 24-27 Sep**, and keep promotions out of it or the read is blurred by code version. The two-week gate is 27 Sep. Not yet registered — do it before 24 Sep.
- Nightly 00:07Z: iron-funnel line (`~/digest/ironfunnel.log`). Last: 59 raw iron / 52 ingots / 3 picks crafted, 18 gone (all during work), iron-pick share 12.5%.
- Tier-1 local analyst every 30 min (`~/digest/*.verdict.json`); tier-0 digest `~/digest/latest.md`. `versions_ok=False` is the known build-suffix false-fail (`<sha>+<buildhash>`), not an alarm.
- Next 72-h program read: schedule from the next promotion.

## CAUTION — a second session was live on this repo today
At 11:26:28Z, while this session was deploying owner-01, another Claude session committed
d10507e to this branch (its own falls-01 decision note). It is the previous evening's operator
session, still alive with a waiter armed for owner-01. Nothing was lost — both accounts of
falls-01 now sit in the registration doc and agree — and the loop's `flock` means only one
canary loop can ever run. But the owner's rotation rule is **one session per day**, and two
sessions holding the same canary is how a decision gets recorded twice or torn down twice.
**A fresh session should confirm no other session is live before acting on the canary.**

## Daily session rotation
- Desktop scheduled task `mcai-daily-session` starts a FRESH session at 06:08 America/Chicago (11:08 UTC). It reads this file first, closes any open canary (**check the ledger and the journal BEFORE acting** — the loop may already have), re-arms the list below, does the queue, rewrites this file.
- `check-open-loop.py` lives at **`/opt/minecraft-ai/scripts/check-open-loop.py` on .31** and must run there with sudo. It is NOT in `~/mcai-analysis`.

## Standing constraints (verbatim from the owner)
- Full autonomy: deploy, canary, promote, tear down without waiting. Wake the owner on every promote or revert and on anything needing a human hand.
- No world changes to fix a bot (sandbox 10.0.0.30:25599 only). Swimming is travel. No 192.168.19x network; no UniFi API on 10.0.0.1; never disable rpcbind; never touch the apt timer. One canary at a time (a bundle counts as one). Teardown is THREE steps. Deploy only via `~/bin/fleet-deploy`. Commit with `git commit -F -` heredocs. Two Codex passes per patch, then a smaller patch. Never share a live canary's inference endpoint/model; ARBITER stays OFF. Never `git add -A bots` in a worktree with a node_modules symlink. Readers include rotated `.gz` generations. logEvent kinds carry a leading underscore in `skill.name`. Lab SSH `mike@10.0.0.31` (bots) / `mike@10.0.0.30` (worlds); the lab key on the mini is `~/.ssh/id_ed25519`. Status reports live in docs/reports. Use `date -u` for clock labels.

## Worktrees
mcai-owner (movement-owner-1, **LIVE canary aa44514**), mcai-falls (falls-path-log, fc28885 — reverted, rebase for falls-02), mcai-owner-f (3b9ff25 = falls tip + owner, unused: falls-01 was not promoted), mcai-deathsites (b1659c0 = main), mcai-rl02 (recovery-ladder-03 = docs/scripts branch), mcai-recovery (iron tools; pooling NOT started), mcai-rllava, mcai-rliron, mcai-rl10, mcai-canopy, mcai-scene (sandbox harness).
Scripts: `~/mcai-analysis` on this Mac (reads); on .31 `~/mcai-analysis` holds drawrec.sh, changerowcheck.py, drawexposure.py, **deathgate.py**, registrations, and **`lib/` — the golden analysis library restored after every deploy**. `/tmp` on .31 mirrors the reads.
Backups made today on .31: `~/telemetry.py.bak-20260918`, `~/verdict.py.bak-20260918-deathgate`, `~/bin/fleet-deploy.bak-20260918`, `~/mcai-analysis/registrations/owner-01.json.bak-20260918`.

## Re-arm on a fresh session (monitors are session-local)
1. **Loop pages AND journal phases**: Monitor tailing **both** `~/digest/page.jsonl` **and `~/canary-journal.jsonl`** on .31, filtered to `verdict|error|flag|PROMOTED|REVERT|deployed|torn-down|KEEP|INCONCLUSIVE|read-|phase`. **Filter on new lines only** (`tail -n 0 -F`) — a naive tail matches yesterday's pages and reads as a fresh alarm.
   **`page.jsonl` ALONE IS NOT A HEARTBEAT** — measured 18 Sep 12:04Z. The loop pages decisions and errors only; a routine read does not page. owner-01's +30 read ran at 11:59:36Z and wrote `VERDICT NOT_YET (+30) :: not readable yet (5.0 bot-h)` to the journal and to `~/canary-loop-<run>.out`, and **nothing to page.jsonl**. A 30-minute watch on page.jsonl straddling that read therefore expired with zero events — which is indistinguishable from a loop that died quietly between decisions. The journal records every phase (`preflight-ok`, `draw`, `drawn`, `deployed`, `read-30`, `read-90`, ...), so it is the heartbeat; page.jsonl is the alarm. Watch both, and treat unexpected silence as a reason to `pgrep -f canary-loop` rather than as good news.
2. **Local analyst**: Monitor running `bash ~/analystwatch.sh` on .31. **DO NOT FILTER IT ON MESSAGE TEXT.** Measured 15:35Z 18 Sep: a filter added at 12:05Z to drop the expected `OpenLoop: canary aa44514` noise suppressed the 15:30 verdict, which carried **`page_claude: True` and `fleet_healthy: False`** — because that same string appears in the *evidence* field of an anomaly bundled with everything else the verdict says. The suppressed verdict turned out benign (the OpenLoop is what a live canary IS; `gates_tripped: []`, `versions_ok: True`, `exposure_ok: True`), but it was benign by luck, not by the filter's design. Two canaries in a row also shared a sha, so a filter naming one run silently kept matching the next. **The analyst runs every 30 min and its noise is ~20 lines over a canary's life; that is cheaper than one suppressed alarm.** Take it unfiltered and triage by hand.
3. **Death poll**: the loop runs `verdict.py <run> 0 --poll` every 5 min itself and pages on REVERT, so monitor 1 covers it. A separate poll is only needed if the loop is dead.
4. If the loop is dead (`pgrep -f canary-loop`) with a canary declared in the manifest: read `~/canary-journal.jsonl`, take the reads by hand (`bash ~/mcai-analysis/arm-read.sh <M> owner-01 "..."`), record the verdict with `check-open-loop.py --record` under sudo **BEFORE** touching the manifest, then promote or tear down (THREE steps: `sudo /usr/local/sbin/mcai-canary-tree teardown`; clear `canary_pool` and `canary_code_version`; restart the pool's bots 12 s apart; confirm exactly one version is live).
5. **After any deploy, check the analysis library survived** — `python3 -c "import sys; sys.path.insert(0,'/opt/minecraft-ai/scripts'); from lib.telemetry import Events; print(len(Events.load(since_minutes=30).rows))"` on .31. `fleet-deploy` restores it now, but a deploy by any other path will not.
