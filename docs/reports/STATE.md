# STATE — the operator's state file (regenerated at every verdict; a fresh session starts from THIS, not from the handoff history)
_updated 2026-09-18 11:30 UTC_

## Fleet
- 80 bots / 16 Peaceful worlds. Baseline **b1659c0** fleet-wide; canary **owner-01 (aa44514)** live on hive-b,hive-c since 11:25:22Z. main = b1659c0. Previous mains: main-pre-2026-09-17 (1d6c97d), main-pre-2026-09-16b (08a3da2), main-pre-2026-09-16 (426058d).
- Health at 11:00Z digest: one version live pre-canary, 80 bots, 0 deaths in 2 h, 0 immobile, items/bot-h 31.5, decisions/bot-h 45.3.
- **keepInventory=true and doImmediateRespawn=true on every world** (deliberate, place-town.py): deaths cost time, not items. Owner decision 18 Sep 02:10Z: it stays ON through the seed canary's 72 h, then is turned OFF fleet-wide as its own registered program change (a world rule, never a canary).

## Live canary — owner-01 (aa44514), MOVEMENT OWNER step 1
- Deployed 11:25:22Z 18 Sep on **hive-b,hive-c** (drawn at deploy; one pool per inference half; placebo-a/-b excluded on the 12-h ledger rule). Under the host loop (`~/canary-loop.sh owner-01`, pid alive, log `~/canary-loop-owner-01.out`). Preflight changerowcheck PASSED (escape_rung and safe_hold both silent on the baseline; positive control 189,544 rows / 80 bots / 110 kinds). Suite 192/192 on aa44514.
- **Reads (UTC): +30 11:55 · +90 12:55 · +180 14:25 · +360 17:25 · extension +540 20:25 · +720 23:25 · deadline +780 00:25 on 19 Sep.** The loop reads, decides, promotes or tears down on its own and pages every step.
- Registration `~/mcai-analysis/registrations/owner-01.json` (v20 in the registration doc). **Amended before deploy: `final_on_zero_exposure` KEEP_ON_SAFETY -> INCONCLUSIVE** (backup `owner-01.json.bak-20260918`), the same correction made to -13c — the exposure interlock says zero exposure is never a KEEP.
- Own lines: `control_own_rows <= 0` REVERT; `refused_actuator_per_bh_canary <= 30` WATCH; `hold_share_canary <= 0.5` WATCH. Exposure: >= 1 entombed episode on a canary bot.
- Previous: **falls-01 REVERT** (see below), -13c KEEP and promoted.

## Closed today
- **falls-01 (fc28885) — REVERT 04:39:10Z, torn down 04:42:45Z, by the loop, before this session started.** Ledger clean, no open loop, one version live. The verdict stands; the read of why is in the registration doc under v21, and it is the reason the death gate changed.
- **THE DEATH GATE REVERTED A CHANGE THAT COULD NOT HAVE CAUSED IT.** falls-01 was report-only — a `fall_path` row at a fall, no behaviour change. Its two deaths were idle (one drowning, one unknown, **zero falls**); all **three control deaths in the same window were idle drownings**, the same background mechanism. Denominators: canary 2 in 23.9 bot-h (0.084/bh), control 3 in 168 bot-h (0.018/bh); point ratio 4.7x, lower 95% bound **0.58x**.
- **falls harvest (31 `fall_path` rows, placebo-a 19 / placebo-b 12, zero fall deaths):** the liquid-dropdown hypothesis is **dead** (liqdrop=true on all 31, landing cell water on **1**). 29 of 31 are the planner's own 4-5-block drops at 1-2 hp, which is policy (maxDropDown 6), not defect. **One overshoot** (placebo-b-Comet 03:49:24, planned 5.3, hp -9 => ~12 blocks — pathfinder momentum, issue #31). **One real infinite-liquid dropdown** (placebo-b-Echo 03:14:22, planned max **49**, 1 drop beyond mdd=6, into water, cost 2 hp — the mechanism exists, is rare, and is harmless when the water is there). **The fatal 35-block class is still unmeasured**: it needs a fall DEATH, and 2.4 h on 10 bots produced none.

## Rules changed today (both PROSPECTIVE, both in docs/reports/recovery-ladder-registration.md)
- **v21 — the death gate's 1.25x test moves to the LOWER BOUND of the rate ratio.** The owner's two-death floor is untouched. The gate is polled every 5 min for up to 9 h (~108 looks) at an event with a null expectation under one death per canary, so the point ratio clears 1.25x on Poisson noise. Calibrated before it may decide anything (`scripts/calibrate_deathgate.py`): **false revert 43.7% -> 5.0%**, detection at 3x 95% -> 51%, at 5x 99.9% -> 88.6%, at swim_to scale (10x) 100% -> 99.9%. The 43.7% is the positive control — it reproduces the 46% measured on 2026-09-13. Rejected alternative: excusing a death whose cause class also appears in control, which would have excused swim_to. `scripts/deathgate.py` (pure function, 15 tests, 5 mutants killed), wired into `~/verdict.py` on .31 (backup `~/verdict.py.bak-20260918-deathgate`).
- **The analysis library no longer belongs to the deploy tree.** Every read does `sys.path.insert(0, '/opt/minecraft-ai/scripts')`, but `/opt/minecraft-ai` is a deploy artefact that `fleet-deploy` checks out and `deploy-fleet.sh` git-resets — so the library silently became whatever the last canary carried. Measured today: deploying owner-01 reverted a telemetry fix installed 40 minutes earlier and would have broken owner-01's own +30 read. **Golden copy now lives at `~/mcai-analysis/lib/`, and `~/bin/fleet-deploy` restores it after every deploy** (backup `~/bin/fleet-deploy.bak-20260918`). Verified in place after the owner-01 deploy.

## Instrument fixed today — `Events.load()` was refusing EVERY call
`scripts/lib/telemetry.py`, commit c79d57b. The OOM guard summed the whole glob before applying the window, so 1124 rotated generations (0.44 GB on disk, counted at 25x) estimated 11.4 GB against 1.9 GB actually there, and `since_minutes=30` raised exactly like a 30-day walk. It broke between 04:39Z and 11:10Z today and took fallread, deathread, canary-report and verdict.py's evidence objects with it — i.e. it would have broken the next canary's reads entirely. Fix: one `predates_window()` shared by the estimate and the walk, one frozen glob for both, a runtime decompressed-byte budget, `WalkTooWide` re-raised instead of swallowed, **one bad line now costs a line rather than the rest of the file** (Codex reproduced valid / `[]` / valid returning ONE row), unreadable files and mid-walk rotation reported rather than silent, naive datetime bounds refused. 16 behavioural cases, six mutants killed. Two Codex passes folded; two of the tests were caught passing for the wrong reason by their own mutants first.

## Queue (histogram order)
1. **owner-01 verdict** (the loop owns it; reads through 00:25Z 19 Sep). While it is deployed and unread, no new analysis starts.
2. **Seed canary** — world change, two pools, `scripts/reseed-pool.sh` v3 (journaled, resumable, dry-runs clean). Owner decided 18 Sep 02:10Z: run it unsupervised, **one pool at a time** — pool one, verify 5/5 in-world and a clean digest an hour later, then pool two; stop on any stage that fails to verify; nothing is deleted (archives `.pre-reseed-<ts>`). Re-seeded pools are excluded from code draws for 72 h via `~/mcai-analysis/draw-exclude.txt`. Read 24/48/72 h by pool set and promotion epoch; the reseed-plus-reset confound is registered in docs/reports/seed-canary-registration.md. **Run it after the owner-01 verdict, not alongside** — re-seeded pools would sit in owner-01's DiD control arm.
3. **falls-02 — run the falls instrument FLEET-WIDE, not as a canary.** Today's run answered the injury question and could not touch the fatal one: fall deaths are too rare for 10 bots. It is behaviour-inert, so the exposure argument beats the canary argument. Rebase the falls tip onto whatever is baseline then; register under v21.
4. **The unplanned/overshoot fall class** — the design target the harvest actually identified (a 4-5-block fall with NO planned drop, and the planned-5-fell-12 overshoot), not the planner's drop policy.
5. **keepInventory OFF fleet-wide** as its own registered program change, after the seed canary's 72 h; expect the iron-pickaxe share to drop when it flips, and say so in the first read.
6. **Pocket-rung block floor** (need+4 with 5 held; both reviews said need+1 cannot fund a side exit).
7. **Calibrate `drawexposure.py`** — still REPORT-only; 1 refusal, 1 false positive. Sweep the lookback (6/12/24 h) against draws whose exposure outcome is known (-13b, -13c, 1011b). Do not let it narrow `elig()` until then. Harmless today: owner-01 declares no `draw_exposure`, so the loop's `$RUN` argument fell back to the v8 filter.
8. **Pooling rule -12** (iron; docs/reports/pooling-rule-design.md v3; build NOT started).
9. Housekeeping: delete `~/mcai-analysis/registrations/exptest.json` on .31.

## Rules in force (docs/reports/recovery-ladder-registration.md)
v12 linkage, v14c, v15c movement guards (calibrated 2% false-revert / 97% detection), v16 (calibrate before revert; trace refusals to fallbacks; histogram slot order; bundles of two disjoint changes), v17/v18, v19 (a change/linkage row must DISCRIMINATE — `changerowcheck.py` pre-deploy, `license_change_rows()` at read time, the single-death rung-linkage override demoted to WATCH), **v21 (the death gate on the lower bound)**, the owner's death gate floor of **two** canary deaths, draws at deploy (12-h ledger exclusions, ±25% then ±40%, 5080 half, never placebo-c/isolated), `fleet-deploy` refuses a `--pool` sha not descending from `declared_code_version`.

## Standing wake-ups
- Nightly 00:07Z: iron-funnel line (`~/digest/ironfunnel.log`). Last: 59 raw iron / 52 ingots / 3 picks crafted, 18 gone (all during work), iron-pick share 12.5%.
- Tier-1 local analyst every 30 min (`~/digest/*.verdict.json`); tier-0 digest `~/digest/latest.md`. `versions_ok=False` is the known build-suffix false-fail (`<sha>+<buildhash>`), not an alarm.
- Next 72-h program read: schedule from the next promotion.

## Daily session rotation
- Desktop scheduled task `mcai-daily-session` starts a FRESH session at 06:08 America/Chicago (11:08 UTC). It reads this file first, closes any open canary (**check the ledger and the journal BEFORE acting** — the loop may already have), re-arms the list below, does the queue, rewrites this file.
- `check-open-loop.py` lives at **`/opt/minecraft-ai/scripts/check-open-loop.py` on .31** and must run there with sudo. It is NOT in `~/mcai-analysis`.

## Standing constraints (verbatim from the owner)
- Full autonomy: deploy, canary, promote, tear down without waiting. Wake the owner on every promote or revert and on anything needing a human hand.
- No world changes to fix a bot (sandbox 10.0.0.30:25599 only). Swimming is travel. No 192.168.19x network; no UniFi API on 10.0.0.1; never disable rpcbind; never touch the apt timer. One canary at a time (a bundle counts as one). Teardown is THREE steps. Deploy only via `~/bin/fleet-deploy`. Commit with `git commit -F -` heredocs. Two Codex passes per patch, then a smaller patch. Never share a live canary's inference endpoint/model; ARBITER stays OFF. Never `git add -A bots` in a worktree with a node_modules symlink. Readers include rotated `.gz` generations. logEvent kinds carry a leading underscore in `skill.name`. Lab SSH `mike@10.0.0.31` (bots) / `mike@10.0.0.30` (worlds); the lab key on the mini is `~/.ssh/id_ed25519`. Status reports live in docs/reports. Use `date -u` for clock labels.

## Worktrees
mcai-owner (movement-owner-1, **LIVE canary aa44514**), mcai-falls (falls-path-log, fc28885 — reverted, rebase for falls-02), mcai-owner-f (3b9ff25 = falls tip + owner, unused: falls-01 was not promoted), mcai-deathsites (b1659c0 = main), mcai-rl02 (recovery-ladder-03 = docs/scripts branch), mcai-recovery (iron tools; pooling NOT started), mcai-rllava, mcai-rliron, mcai-rl10, mcai-canopy, mcai-scene (sandbox harness).
Scripts: `~/mcai-analysis` on this Mac (reads); on .31 `~/mcai-analysis` holds drawrec.sh, changerowcheck.py, drawexposure.py, **deathgate.py**, registrations, and **`lib/` — the golden analysis library restored after every deploy**. `/tmp` on .31 mirrors the reads.
Backups made today on .31: `~/telemetry.py.bak-20260918`, `~/verdict.py.bak-20260918-deathgate`, `~/bin/fleet-deploy.bak-20260918`, `~/mcai-analysis/registrations/owner-01.json.bak-20260918`.

## Re-arm on a fresh session (monitors are session-local)
1. **Loop pages**: Monitor tailing `~/digest/page.jsonl` on .31 filtered to `verdict|error|flag|PROMOTED|REVERT|deployed|torn-down`. **Filter on new lines only** (`tail -n 0 -F`) — a naive tail matches yesterday's pages and reads as a fresh alarm.
2. **Local analyst**: Monitor running `bash ~/analystwatch.sh` on .31.
3. **Death poll**: the loop runs `verdict.py <run> 0 --poll` every 5 min itself and pages on REVERT, so monitor 1 covers it. A separate poll is only needed if the loop is dead.
4. If the loop is dead (`pgrep -f canary-loop`) with a canary declared in the manifest: read `~/canary-journal.jsonl`, take the reads by hand (`bash ~/mcai-analysis/arm-read.sh <M> owner-01 "..."`), record the verdict with `check-open-loop.py --record` under sudo **BEFORE** touching the manifest, then promote or tear down (THREE steps: `sudo /usr/local/sbin/mcai-canary-tree teardown`; clear `canary_pool` and `canary_code_version`; restart the pool's bots 12 s apart; confirm exactly one version is live).
5. **After any deploy, check the analysis library survived** — `python3 -c "import sys; sys.path.insert(0,'/opt/minecraft-ai/scripts'); from lib.telemetry import Events; print(len(Events.load(since_minutes=30).rows))"` on .31. `fleet-deploy` restores it now, but a deploy by any other path will not.
