## Rules in force (docs/reports/recovery-ladder-registration.md)
**v30 (a registration whose LAST READ MINUTE is not strictly inside its deadline is REFUSED AT LAUNCH: `deadline_min` > `max(read_minutes)` + a 30 min read grace. drop5-01 registered read_minutes [30,90,180,360] AND deadline_min 360, so the loop reached the +360 read at 362 min elapsed, fired containment and recorded INCONCLUSIVE with the final read NEVER TAKEN — no drop5read-360.json, no immobiledid-360.json, while exposure 101/60, linkage and safety were all clean. Enforced in `canary-loop.sh` via `v30check.py`, which is the only place the loss is preventable; `verdict.py` additionally reports it on every read. 3 of 18 registrations on file carried the pattern, all written in the last two days) — LIVE, PROSPECTIVE from 2026-09-25**, **v29 (a calibration offered as `evidence: calibrated` must report `false_trip_rate_ci_upper`, and it is the CONFIDENCE BOUND, not the point estimate, that must clear `CAL_MAX_FTR` — absent, non-numeric, or below its own point estimate is a REFUSAL. A registration naturally sets `at_threshold` at its calibration's own p95, which makes the realised false-trip rate 0.0500 BY CONSTRUCTION, so v28's `ftr > 0.05` could never fire and waved through every line calibrated that way. `CAL_MAX_AGE_H = 48` is now MEASURED, not a placeholder: 19 read anchors, 200 application draws each, 48 h passes at a 9.5% CI upper and 72 h is the first to cross at 10.7%; registered WITH the honest caveat that at zero age the worst anchor still realises 14.5%, so an age limit buys little — read-era variance 3.5 pp is beyond its reach. Measured over 18 registrations, ZERO have ever carried `evidence: calibrated`, so v29 has never decided a canary) — LIVE, PROSPECTIVE from 2026-09-25**, **v28c (a deciding calibrated own-line must be `form: did`; a canary-only LEVEL may never revert — measured, an identical-code level line false-tripped 11.2–19.8% then 48.8–53.0% as the fleet's no-path rate doubled) — LIVE**, **v28b (a calibration must report `nonzero_draws`; below 10% it is DEGENERATE and refused, because a treatment-only metric scores 0% false-trips for free) — LIVE**, **v28 (a change's OWN alarm may revert its canary, but ONLY with `evidence: calibrated` — `at_threshold` equal to the registered value, `false_trip_rate` ≤ 0.05, `over_reads: true`, `measured_at` within `CAL_MAX_AGE_H` (48 h, MEASURED under v29 on 2026-09-24 — it was a placeholder when v28 was registered and is no longer), `tool` and `draws` named; any unmet BLOCKS KEEP as INCONCLUSIVE, never REVERT; AT MOST ONE such deciding line per canary, because 9 reads × 5% is 37%) — LIVE, PROSPECTIVE from 2026-09-24**, **v27b (the linkage window is calibrated at 60 s — background false-link 5.0% vs 20.0% at 600 s; the canary's own emission rate is REPORTED, NOT ENFORCED) — LIVE**, v27 (the verdict SAYS when linkage was unavailable, so an inert sensitive path cannot read as a clean one), **v26b (the death-gate in-window randomization p is REPORT-ONLY; `p_vetoes=True` required before it may change a verdict, because as a veto it discarded rl-08b) — LIVE**, v26 (that p is ranked on the canary death RATE, not the count, permuted at the unit of ASSIGNMENT from `canary_split`), **v25 (a typed threshold is NOT evidence: a deciding own-line must declare `evidence: 'defect'` or a present `support` p ≤ max, else the line FAILS, is reported, and the verdict is INCONCLUSIVE — never a fall-through to KEEP) — LIVE**, **v24 (an own-line value that is None, NaN or ±inf is UNREADABLE, not a failure) — LIVE**, **v23 (no single canary death reverts by ANY path; gates carry `role: deciding|tripwire`) — LIVE in the verdict path**, v22 WITHDRAWN unregistered, v12 linkage, v14c, v15c movement guards (calibrated 2% false-revert / 97% detection), v16 (calibrate before revert; trace refusals to fallbacks; histogram slot order; bundles of two disjoint changes), v17/v18, v19 (a change/linkage row must DISCRIMINATE — `changerowcheck.py` pre-deploy, `license_change_rows()` at read time), **v21 (the death gate on the LOWER BOUND of the rate ratio — OPEN OWNER QUESTION: backtested, it trips on 0 of all 15 death-involved reverts, so its false-positive win was bought with real detection)**, the owner's floor of **two** canary deaths, draws at deploy (12-h ledger exclusions, ±25% then ±40%, per inference half, never placebo-c/isolated), `fleet-deploy` refuses a `--pool` sha not descending from `declared_code_version`, `CANARY_ENV` + the `/proc/<pid>/environ` assertion so a flagged canary cannot ship inert. Death-gate exposure is MEASURED, never the old `canary_bot_h × 7` (mutant-pinned).
